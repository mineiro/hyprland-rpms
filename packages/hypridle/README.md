# hypridle

Starter packaging directory for `hypridle` in this monorepo.

Current status:
- starter spec added (adapted from the `solopasha/hyprlandRPM` baseline)
- target version set to `0.1.7` (current upstream stable at the time of scaffolding)
- current spec uses a documented bundled `sdbus-cpp` source path (temporary policy)
