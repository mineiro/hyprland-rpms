# astal-cava

Fedora RPM packaging for Astal's CAVA bindings.

Current packaging target:

- pinned `Aylur/astal` snapshot commit `41b5029` (`snapshot_date 20260319`)
- provides the `AstalCava-0.1` GIR/typelib
- depends on the packaged system `libcava` library instead of vendoring it
