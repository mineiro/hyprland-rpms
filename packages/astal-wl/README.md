# astal-wl

Fedora RPM packaging for Astal's generic Wayland helper library.

Current packaging target:

- pinned `Aylur/astal` snapshot commit `41b5029` (`snapshot_date 20260319`)
- provides the `AstalWl-0.1` GIR/typelib
- used as the shared Wayland support library for `astal-river`
