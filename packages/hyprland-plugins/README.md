# hyprland-plugins

Fedora RPM packaging for `hyprland-plugins` (official Hyprland plugin bundle).

Current packaging target:

- upstream tag `v0.53.0` (compatibility target for the current `hyprland 0.53.x` stack)

