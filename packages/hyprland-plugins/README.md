# hyprland-plugins

Fedora RPM packaging for `hyprland-plugins` (official Hyprland plugin bundle).

Current packaging target:

- upstream tag `v0.53.0` (pinned plugin compatibility target; Hyprland is currently shipped at `0.54.0` without forced plugin ABI compatibility)
