# appmenu-glib-translator

Fedora RPM packaging for the AppMenu GLib translator library from
`rillian-la-te/vala-panel-appmenu`.

Current packaging target:

- upstream tag `25.04`
- provides the upstream-installed `AppmenuGLibTranslator-24.02` GIR/typelib
- used as the system dependency for `astal-tray`
