# python-materialyoucolor

Fedora RPM packaging for `materialyoucolor`.

Current packaging target:

- upstream release `3.0.2` from `T-Dynamos/materialyoucolor-python`
- Python library package built from the PyPI sdist
- ships the upstream pybind11-backed quantization extension for native speed
