# swappy

Fedora RPM packaging for `swappy`.

Current packaging target:

- upstream release `1.8.0` from `jtheoof/swappy`
- Wayland-native snapshot editor built with Meson
- based on Fedora's existing `swappy` packaging, updated for the current upstream release
