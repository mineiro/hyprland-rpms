# xdg-desktop-portal-hyprland (starter package)

This package is usually easier to stabilize than `hyprland` itself, so it is a good second package after the core `hypr*` libraries and `hyprland`.

Reference source used for this scaffold:

- `solopasha/hyprlandRPM/xdg-desktop-portal-hyprland/xdg-desktop-portal-hyprland.spec`

Verify `Epoch`, `Version`, `Source`, and bundled dependency policy before your first release.

