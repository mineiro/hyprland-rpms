# app2unit

Fedora RPM packaging for `app2unit`.

Current packaging target:

- upstream release `1.4.0` from `Vladimir-csp/app2unit`
- POSIX shell launcher package with generated `scdoc` man page
- installs `app2unit` plus the upstream `-open*` and `-term*` helper links
