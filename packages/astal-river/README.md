# astal-river

Fedora RPM packaging for Astal's River WM bindings.

Current packaging target:

- pinned `Aylur/astal` snapshot commit `41b5029` (`snapshot_date 20260319`)
- provides the `AstalRiver-0.1` GIR/typelib
- depends on `astal-wl` and `wl-vapi-gen` for generated Wayland protocol bindings
