# waybar

Fedora RPM packaging for `waybar`.

Current packaging target:

- upstream release `0.15.0` from `Alexays/Waybar`
- stable `waybar` package (not `waybar-git`) to provide a newer build than the
  Fedora default repo version when needed
